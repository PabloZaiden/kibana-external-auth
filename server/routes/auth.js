export default function (server) {

// if needed, new routes can be added here
/*
  server.route({
    path: '/api/auth_plugin/auth',
    method: 'GET',
    handler(req, reply) {
      reply({ time: (new Date()).toISOString() });
    }
  });
*/
};
